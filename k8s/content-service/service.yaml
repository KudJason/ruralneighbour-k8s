apiVersion: v1
kind: Service
metadata:
  name: content-service
  namespace: default
  labels:
    app: content-service
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: content-service











