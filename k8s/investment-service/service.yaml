apiVersion: v1
kind: Service
metadata:
  name: investment-service
  namespace: default
  labels:
    app: investment-service
    tier: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: investment-service






