# Import all models to register them with SQLAlchemy
from app.models.address import UserAddress

# This file is used to import all models so they are registered with SQLAlchemy
# Import this file in main.py after creating the Base
